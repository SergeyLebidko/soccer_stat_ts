import React from 'react';
import {HashRouter} from 'react-router-dom';
import './App.scss';

const App: React.FC = () => {
  return (
    <HashRouter>
      Soccer Stat
    </HashRouter>
  );
};

export default App;
