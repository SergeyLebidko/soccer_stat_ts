import React from 'react';
import {HashRouter, Routes} from 'react-router-dom';
import './App.scss';

const App: React.FC = () => {
  return (
    <HashRouter>
      <Routes>

      </Routes>
    </HashRouter>
  );
};

export default App;
